using MassTransit;

namespace MikoIam.Workflows.MassTransit
{
    public static class ReceiveEndpointConfiguratorExtensions
    {
        public static void Workflow(this IReceiveEndpointConfigurator configurator, object workflow)
        {
            
        }
    }
}